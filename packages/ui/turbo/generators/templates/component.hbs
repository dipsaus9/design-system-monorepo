import type { ReactNode } from "react";

interface {{ camelCase name }}Props {
  children?: ReactNode;
}

export function {{ camelCase name }}({ children }: {{ camelCase name }}Props) {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};
