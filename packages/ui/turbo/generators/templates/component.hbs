import { cx } from "class-variance-authority";

import { forwardRef } from "../helpers/forwardRef";

export interface {{ pascalCase name }}Props {}

const defaultAs = "div" as const; // TODO: Change when ready

export const {{ pascalCase name }} = forwardRef<{{ pascalCase name }}Props, typeof defaultAs>(
  ({ children, className, as, ...props }, ref) => {
    const Component = as ?? defaultAs;

    return (
      <Component ref={ref} {...props} className={cx("", className)}>
        {children}
      </Component>
    );
  }
);
